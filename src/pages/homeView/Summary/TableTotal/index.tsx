import { Card, CardContent, CardHeader } from '@/components';
import React from 'react';

export const TableTotal: React.FC = () => {
  return (
    <Card className="h-full">
      <CardHeader>Total de ventas </CardHeader>
      <CardContent className=""></CardContent>
    </Card>
  );
};
