import { Card, CardHeader } from '@components';
import React from 'react';

export const ImportantDates: React.FC = () => {
  return (
    <Card className="h-full">
      <CardHeader>Fechas importantes a tener en cuenta</CardHeader>
    </Card>
  );
};
